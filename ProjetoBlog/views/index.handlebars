<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao Blog do Node!</h1>
  <p class="lead">Esta é uma página utilizada para estudo e prática de Node JS</p>
  <hr class="my-4">
  <p>Através de um curso online de Node JS, esta página está sendo desenvolvida e aprimorada.</p>
  <a class="btn btn-success btn-lg" href="/usuarios/login" role="button">Login</a>
  <span style="font-size:25px">/</span>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<h2>Postagens recentes:</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small>
        <br>
        <small>Data da publicação: {{data}}</small>
    </div>    

</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}
