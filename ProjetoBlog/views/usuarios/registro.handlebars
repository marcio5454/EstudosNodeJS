<h3>Crie sua conta hoje:</h3>
<div class="card">
    <div class="card-body">
        <form action="" method="POST">
            <label for="nome">Nome:</label>
            <input type="text" name="nome" class="form-control" value="{{registro.nome}}" required>
            <label for="enail">Email:</label>
            <input type="email" name="email" class="form-control" value="{{registro.email}}" required>
            <label for="senha">Senha:</label>
            <input type="password" name="senha" class="form-control" value="{{registro.senha}}" required>
            <label for="senha2">Repita a sua senha:</label>
            <input type="password" name="senha2" class="form-control" value="{{registro.senha2}}" required>
            <button type="submit" class="btn btn-success mt-4">Criar Conta</button>
        </form>
    
    </div>
</div>